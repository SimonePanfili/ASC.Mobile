<?xml version="1.0" encoding="utf-8" ?>
<common:ContentPageBase xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:common="clr-namespace:ASC.Mobile.Common"
             x:Class="ASC.Mobile.Views.CorsoPage"
             BindingContext="{Binding Source={StaticResource Locator}, Path=CorsoViewModel}"
             Title="Dettaglio Corso">

  <Grid>
    <ScrollView Margin="12" Padding="0,0,20,0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Text="Codice:" />
        <Label Grid.Column="1" Text="{Binding Corso.Codice}" />

        <Label Text="Descrizione:" Grid.Row="1" />
        <Label Grid.Row="1" Grid.Column="1" Text="{Binding Corso.Descrizione}" />

        <Label Text="Organizzatore:" Grid.Row="2" />
        <Label Grid.Row="2" Grid.Column="1" Text="{Binding Corso.Organizzatore}" />

        <Label Text="Disciplina:" Grid.Row="3" />
        <Label Grid.Row="3" Grid.Column="1" Text="{Binding Corso.Disciplina}" />

        <Label Text="Qualifica:" Grid.Row="4" />
        <Label Grid.Row="4" Grid.Column="1" Text="{Binding Corso.Qualifica}" />

        <StackLayout Orientation="Horizontal" Grid.Row="5" Grid.ColumnSpan="2" Margin="0,20,0,0">
          <Image VerticalOptions="Start">
            <Image.Source>
              <OnPlatform x:TypeArguments="ImageSource" iOS="calendar.png" Android="calendar.png" WinPhone="Assets/Views/calendar.png" />
            </Image.Source>
          </Image>
          <Label Text="{Binding Corso.Svolgimento}" Style="{StaticResource ListLabelStyle}" />
        </StackLayout>

        <StackLayout Orientation="Horizontal" Grid.Row="6" Grid.ColumnSpan="2" Margin="0,20,0,0">
          <Image VerticalOptions="Start">
            <Image.Source>
              <OnPlatform x:TypeArguments="ImageSource" iOS="pin.png" Android="pin.png" WinPhone="Assets/Views/pin.png" />
            </Image.Source>
          </Image>
          <Label Text="{Binding Corso.Address.FullAddress}" />
        </StackLayout>
      </Grid>
    </ScrollView>

    <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}">
      <ActivityIndicator.HorizontalOptions>
        <OnPlatform x:TypeArguments="LayoutOptions" iOS="CenterAndExpand" Android="CenterAndExpand" WinPhone="FillAndExpand" />
      </ActivityIndicator.HorizontalOptions>
      <ActivityIndicator.VerticalOptions>
        <OnPlatform x:TypeArguments="LayoutOptions" iOS="CenterAndExpand" Android="CenterAndExpand" WinPhone="FillAndExpand" />
      </ActivityIndicator.VerticalOptions>
    </ActivityIndicator>
  </Grid>

</common:ContentPageBase>