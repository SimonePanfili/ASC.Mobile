<?xml version="1.0" encoding="utf-8" ?>
<common:ContentPageBase xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:common="clr-namespace:ASC.Mobile.Common"
             xmlns:behaviors="clr-namespace:Behaviors;assembly=Behaviors"
             x:Class="ASC.Mobile.Views.ElencoNegoziPage"
             BindingContext="{Binding Source={StaticResource Locator}, Path=ElencoNegoziViewModel}"
             Title="Negozi">

  <Grid>
    <ListView Margin="0,3,0,0" x:Name="negoziListView" ItemsSource="{Binding Negozi}" HasUnevenRows="True"
              IsPullToRefreshEnabled="True" RefreshCommand="{Binding RefreshCommand}"
              CachingStrategy="RecycleElement">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout>
              <Frame Padding="5" HasShadow="true" OutlineColor="Gray">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>

                  <Image VerticalOptions="Start" Margin="0,0,4,0" HeightRequest="48" WidthRequest="48"
                         Source="{Binding ImageSource}">
                  </Image>

                  <StackLayout Grid.Column="1">
                    <Label Text="{Binding Denominazione}" Style="{StaticResource TitleListLabelStyle}" />
                    <StackLayout Orientation="Horizontal" Margin="0,20,0,0">
                      <Image>
                        <Image.Source>
                          <OnPlatform x:TypeArguments="ImageSource" iOS="pin.png" Android="pin.png" WinPhone="Assets/Views/pin.png" />
                        </Image.Source>
                      </Image>
                      <Label Text="{Binding Address.FullAddress}" />
                    </StackLayout>
                    <StackLayout Orientation="Horizontal">
                      <Image>
                        <Image.Source>
                          <OnPlatform x:TypeArguments="ImageSource" iOS="web.png" Android="web.png" WinPhone="Assets/Views/web.png" />
                        </Image.Source>
                      </Image>
                      <Label Text="{Binding WebSite}" />
                    </StackLayout>
                    <StackLayout Orientation="Horizontal">
                      <Image>
                        <Image.Source>
                          <OnPlatform x:TypeArguments="ImageSource" iOS="phone.png" Android="phone.png" WinPhone="Assets/Views/phone.png" />
                        </Image.Source>
                      </Image>
                      <Label Text="{Binding PhoneNumber}" />
                    </StackLayout>
                  </StackLayout>
                </Grid>
              </Frame>
              <BoxView HeightRequest="1" IsEnabled="False" />
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>

      <ListView.Behaviors>
        <behaviors:EventHandlerBehavior EventName="ItemTapped">
          <behaviors:InvokeCommandAction Command="{Binding ItemTappedCommand}" Converter="{StaticResource ItemTappedConverter}" />
        </behaviors:EventHandlerBehavior>
      </ListView.Behaviors>
    </ListView>

    <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}">
      <ActivityIndicator.HorizontalOptions>
        <OnPlatform x:TypeArguments="LayoutOptions" iOS="CenterAndExpand" Android="CenterAndExpand" WinPhone="FillAndExpand" />
      </ActivityIndicator.HorizontalOptions>
      <ActivityIndicator.VerticalOptions>
        <OnPlatform x:TypeArguments="LayoutOptions" iOS="CenterAndExpand" Android="CenterAndExpand" WinPhone="FillAndExpand" />
      </ActivityIndicator.VerticalOptions>
    </ActivityIndicator>

  </Grid>

</common:ContentPageBase>